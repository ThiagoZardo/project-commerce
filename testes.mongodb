use("commerce")
db.produtos.countDocuments({ nome: { $regex: /Mc/i } });


// Mc 
db.products.find({ sku: { $regex: /^Mc/i } });

use("commerce")
db.produtos.countDocuments({});

use("commerce")
db.produtos.find({});