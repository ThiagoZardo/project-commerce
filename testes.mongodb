use("commerce")
db.produtos.updateMany(
  { $not: { nome: { $eq: "McChicken" } } },
  { $push: { ingredientes: "ketchup"  } }
);


use("commerce")
db.produtos.find({}, { _id: 0, nome: 1, ingredientes: 1 });

use("commerce")
db.produtos.find({});


use("commerce")
db.produtos.find(
  { nome: { $not: { $eq:"Big Mac", "McChicken" } } }, 
  { _id: 0, nome: 1, curtidas: 1, vendidos: 1 });

use("commerce")
db.produtos.find({});