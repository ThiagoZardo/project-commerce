use("commerce")
db.produtos.find(
  { $expr: { $gt: [ "$curtidas", "$vendidos" ] } },
  { _id: 0, nome: 1 }
);

// Mc 
db.products.find({ sku: { $regex: /^Mc/i } });

use("commerce")
db.produtos.countDocuments({});

use("commerce")
db.produtos.find({});