use("commerce")
db.produtos.updateMany(
  { nome: "Big Mac" },
  { $unset: { curtidas: "" } }
);
db.produtos.find({ nome: { $not: { $eq: "Big Mac" } } }, { _id: 0, nome: 1, curtidas: 1 });

// Mc 
db.products.find({ sku: { $regex: /^Mc/i } });

use("commerce")
db.produtos.countDocuments({});

use("commerce")
db.produtos.find({});