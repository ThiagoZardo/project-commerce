use("commerce")
db.produtos.find(
  { vendidos: { $mod: [5, 0] } },
  { _id: 0, nome: 1, vendidos: 1 }
);

// Mc 
db.products.find({ sku: { $regex: /^Mc/i } });

use("commerce")
db.produtos.countDocuments({});

use("commerce")
db.produtos.find({});